import { RGBA } from "@opentui/core";

export const PALETTE = {
  WHITE: RGBA.fromValues(1, 1, 1),
  BLACK: RGBA.fromValues(0, 0, 0),
  GRAY: RGBA.fromValues(0.5, 0.5, 0.5),
  DARK_GRAY: RGBA.fromValues(0.2, 0.2, 0.2),
  RED: RGBA.fromInts(255, 0, 0),
  BLUE: RGBA.fromInts(0, 0, 255),
  GREEN: RGBA.fromInts(0, 255, 0),
}